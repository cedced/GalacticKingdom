# Godot generated caches (never commit imported artifacts, see assets/README.md)
.godot/

# Per CLAUDE.md Section 7.9
*.db
exports/
.env

# Local editor cruft
*.tmp
*.bak

# Python tool caches
__pycache__/

# Transient generation candidates and Envato downloads awaiting intake (assets/README.md);
# the manifest row is the record, the kept file lands under assets/<kind>/
assets/dump/gen/
assets/dump/envato/

# Local model checkpoints, LoRAs, training sets (tools/gen/models.json is the record)
assets/source/
